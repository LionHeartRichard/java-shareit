spring:
  profiles:
    active: default

logging:
  level:
    org.springframework.web.client.RestTemplate: ERROR
    ru:
      practicum:
        shareit: TRACE

server:
  port: 8080

shareit-server:
  url: http://localhost:9090
